extends ../base

block content
    if errorMessage
        +message("error", errorMessage)
    form(method="POST")
        include ../partials/social-login.pug
        input(name="email", placeholder="Email", type="email", required)
        input(name="username", placeholder="Nickname", type="text", required)
        input(name="password", placeholder="Password", type="password", required)
        input(name="password2", placeholder="Confrim Password", type="password", required)
        input(name="location", placeholder="Location", type="text", required)
        input(type="submit", placeholder="submit", value="Sign Up")
    div.auth-switch
        span Already have an account? 
        a(href="/login") Log in now 
            i.fas.fa-arrow-right