extends base
include mixins/room

block content
    form(method="GET")
        input(name="keyword", placeholder="Search by title", type="text")
        input(type="submit", value="Search now")

    div
        each room in rooms
            +room(room)